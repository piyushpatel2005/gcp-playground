# 3GPP PM Files Processing for Telecom

This has a util file as `GPPXmlFlat.py`. To test out, run below.

```shell
python -m venv env
source env/bin/activate
pip install -r requirements.txt
python driver.py --env local
```